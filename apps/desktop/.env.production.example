## Production — copy to `apps/desktop/.env.production` before `npm run install:mac`
## Bundled into Screen Record.app; points ingest + "Open library" at your deployed gallery.

WEB_APP_BASE_URL="https://recordings.gorillabotlabs.com"
DESKTOP_INGEST_SECRET="paste-the-same-secret-here"

# Optional: override default bucket name
# GCS_BUCKET="screen-record"